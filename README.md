# Jungle.Block Solutions

Premium landing + interactive hero chatbot (static export).

## Dev

```bash
cd web
npm install
npm run dev
```

## GitHub Pages (static)

This project uses Next.js static export.

- Build output is `web/out`
- Configure GitHub Pages to deploy from Actions (recommended)

